<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo</title>
</head>
<body>
    <h1>Things to do today:</h1>
    <ul>
        <% if (todo.length === 0) { %>
            <p>There are no todos! Go for a beer!!! </p>
        <% } else { %>
            <% todo.forEach(item => { %>
                <li>
                        <%= item %>
                </li>
            <% }) %>
            <% } %>
    </ul>    
</body>
</html>
